[build]
# You are already at the root directory, just target the workspace app directly
command = "cd apps/website && ./node_modules/.bin/vue-tsc --build && ../../node_modules/.bin/vite build"

# Point to the exact build folder from the root repository
publish = "apps/website/dist"

[build.environment]
PNPM_FLAGS = "--recursive"
