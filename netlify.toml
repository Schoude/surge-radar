[build]
# Navigate two levels up to the root workspace directory to run pnpm filtered to your app
command = "pnpm --prefix ../.. --filter website build"

# Point directly to the build output inside apps/website
publish = "dist"

[build.environment]
PNPM_FLAGS = "--recursive"
