[build]
# Bypass run-p by calling the individual package scripts sequentially via pnpm run
command = "pnpm --filter website run type-check && pnpm --filter website run build-only"

publish = "apps/website/dist"

[build.environment]
NODE_ENV = "development"
