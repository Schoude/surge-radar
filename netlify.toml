[build]
# The clean, standard workspace command
command = "pnpm --filter website build"

# The correct path to your compiled distribution folder
publish = "apps/website/dist"

[build.environment]
# Forces pnpm to install your devDependencies (Vite, vue-tsc, etc.)
NODE_ENV = "development"
